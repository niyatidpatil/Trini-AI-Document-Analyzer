# ── Trini AI — Python Dependencies ───────────────────────────────────────────
# Pin exact versions for reproducible Docker builds.
# To update: change the version, rebuild the Docker image, and test.

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit==1.35.0

# ── Google Vertex AI (Gemini 2.0 Flash) ──────────────────────────────────────
google-cloud-aiplatform==1.60.0

# ── Vector Database ───────────────────────────────────────────────────────────
pinecone==3.2.2                 # Pinecone cloud vector store (primary DB)
faiss-cpu==1.8.0                # FAISS kept for local MMR re-embedding only

# ── Embeddings & Reranking ────────────────────────────────────────────────────
sentence-transformers==3.0.1    # all-MiniLM-L6-v2 embeddings + cross-encoder reranker

# ── Document Parsing ──────────────────────────────────────────────────────────
pymupdf==1.24.4                 # PyMuPDF (fitz) — block-level PDF parser
pypdf==4.2.0                    # Fallback PDF reader
python-docx==1.1.2              # .docx support
python-pptx==0.6.23             # .pptx support
pytesseract==0.3.10             # OCR fallback (requires Tesseract installed system-wide)
Pillow==10.3.0                  # Image handling for OCR

# ── Data & Tokenisation ───────────────────────────────────────────────────────
tiktoken==0.7.0                 # Token-aware chunking (cl100k_base tokenizer)
pandas==2.2.2                   # Tabular data extraction to text
openpyxl==3.1.2                 # .xlsx engine for pandas
numpy==1.26.4                   # Numerical operations

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1            # Load .env in local development
requests==2.31.0                # HTTP client
beautifulsoup4==4.12.3          # HTML parsing (if needed)